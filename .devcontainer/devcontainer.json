{
  "name": "Fedora Desktop in Codespaces",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [5901],  // Expose the VNC port
  "postCreateCommand": "echo 'VNC server is running on :5901, connect using a VNC client!'"
}